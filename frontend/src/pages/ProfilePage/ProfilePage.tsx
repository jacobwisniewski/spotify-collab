import {Page} from "../Page";
import React from "react";

interface ProfilePageProps {
}

const ProfilePage: Page<ProfilePageProps> = ({ integration, state, dispatch}) => {

    return (
        <div>
            Hello
        </div>
    )
}

export default ProfilePage